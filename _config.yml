markdown: kramdown

repository: https://github.com/Accenture/adop-cartridges-cookbook

collections:
  docs:
      output: true

exclude: [ vendor README.md Gemfile Gemfile.lock Dockerfile docker-compose.yml ]
